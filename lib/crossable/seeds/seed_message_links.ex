defmodule Crossable.Seeds.DialogLinks do
  @moduledoc """
  Seed data for message links.
  """
  require Ecto.Query

  def run() do
  end

  @spec thirty_day_engagement_dialog :: [%{}, ...]
  def thirty_day_engagement_dialog() do
    [
      %{}
    ]
  end
end
